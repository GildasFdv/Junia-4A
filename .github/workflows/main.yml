name: ci
on:
    push:
        branches: [develop]

jobs:
    lint:
        runs-on: [self-hosted, linux, x64]
        steps:
            - name: Checkout repo
              uses: actions/checkout@v3

            - name: lint
              run: find . -name "*.php" -print0 | xargs -0 -n1 php -l

